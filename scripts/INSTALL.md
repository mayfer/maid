# Install maid

```bash
curl -fsSL https://raw.githubusercontent.com/mayfer/maid/main/scripts/web_install.sh | bash
```

This will download the latest release binary for your platform and install it to `~/.local/bin/maid`.

Supported platforms: macOS (Apple Silicon, Intel), Linux (x86_64, ARM64).
